import { InMemoryData.Service } from './in-memory-data.service';

describe('InMemoryData.Service', () => {
  it('should create an instance', () => {
    expect(new InMemoryData.Service()).toBeTruthy();
  });
});
